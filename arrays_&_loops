//Array
var items = ['apple', 2.33, 56, true, 'cherry', 'banana', 'grapes'];

//Iterate or loop through this array
for(let i = 0; i < items.length; i++){
  console.log(items[i]);
}

var items = ["This", "is", "my", "array", "being", "printed."];

for(const index in items){
  console.log(items[index]);
}
